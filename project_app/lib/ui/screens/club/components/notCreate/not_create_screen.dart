import 'package:flutter/material.dart';
import 'components/body.dart';

class NotCreateScreen extends StatelessWidget {
  const NotCreateScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}