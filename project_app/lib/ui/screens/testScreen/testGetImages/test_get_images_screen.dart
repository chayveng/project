import 'package:flutter/material.dart';

import 'body.dart';

class TestGetImagesScreen extends StatelessWidget {
  const TestGetImagesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
