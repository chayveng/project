import 'package:flutter/material.dart';

import 'components/body.dart';

class MultiPicImagesScreen extends StatelessWidget {
  const MultiPicImagesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
