import 'package:flutter/material.dart';

import 'body.dart';

class SectionImagesClub extends StatelessWidget {
  const SectionImagesClub({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
