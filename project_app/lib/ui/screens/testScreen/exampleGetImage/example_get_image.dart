import 'package:flutter/material.dart';

import 'body.dart';

class ExampleGatImage extends StatelessWidget {
  const ExampleGatImage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
