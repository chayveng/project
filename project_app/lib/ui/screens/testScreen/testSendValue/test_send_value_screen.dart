import 'package:flutter/material.dart';

import 'components/body.dart';

class TestSendValueScreen extends StatelessWidget {
  const TestSendValueScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
