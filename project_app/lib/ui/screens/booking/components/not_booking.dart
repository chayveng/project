import 'package:flutter/material.dart';

class NotBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Not booking'),
      ),
    );
  }
}