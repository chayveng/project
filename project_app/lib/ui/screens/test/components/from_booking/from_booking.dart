import 'package:flutter/material.dart';

class FromBooking extends StatefulWidget {
  const FromBooking({Key? key}) : super(key: key);

  @override
  _FromBookingState createState() => _FromBookingState();
}

class _FromBookingState extends State<FromBooking> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [

        ],
      ),
    );
  }
}
